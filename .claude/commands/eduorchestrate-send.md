---
description: EduOrchestrate: preview today's learning email (dry-run, never sends)
allowed-tools: Bash(npx eduorchestrate send-today --dry-run:*)
---
Preview today's EduOrchestrate email. Run `npx eduorchestrate send-today --dry-run` and summarize what would be sent: subject, the top video link, docs, build task, and timebox. This command is preview-only — never run it without --dry-run.
