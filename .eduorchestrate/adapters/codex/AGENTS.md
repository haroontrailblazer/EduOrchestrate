# EduOrchestrate for Codex

Expose or honor `/eduorchestrate`.

If native slash command registration is unavailable, treat `/eduorchestrate`, `eduorchestrate`, or `use EduOrchestrate` as the same workflow.

Use the canonical workflow in `skills/eduorchestrate/SKILL.md` and the local CLI:

```bash
npx eduorchestrate
npx eduorchestrate send-today --dry-run
```
